# Can we have a set with 18 (int) and '18' (str) as a value in it?
# yes, a set can contain both an integer and a string with the same value, as they are considered different types. Here's an example:
my_set = {18, '18'}