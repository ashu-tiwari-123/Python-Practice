list1 = ["ashu","sarthika","shashank",18,15.2354,True,"noob"] # List can contain different data types
print(list1)

#  List Methods
l1 = [0,2,14,2,6,78,45,98,254,12,45,45]
# 1. append() - Adds an element to the end of the list
# l1.append(100)
# 2. extend() - Adds elements from another iterable (like a list) to the end of the list
# l1.extend([100,200,300])
# 3. insert() - Inserts an element at a specified position
# l1.insert(3,101)
# 4. remove() - Removes the first occurrence of a specified element
# l1.remove(45)
# 5. pop() - Removes and returns the element at the specified position (or the last element if no position is specified)
# l1.pop(3)
# 6. clear() - Removes all elements from the list
# l1.clear()
# 7. index() - Returns the index of the first occurrence of a specified element
# index_of_45 = l1.index(45)
# 8. count() - Returns the number of occurrences of a specified element
# count_of_45 = l1.count(45)
# 9. sort() - Sorts the list in ascending order
# l1.sort()
# 10. reverse() - Reverses the order of the elements in the list
# l1.reverse()
# 11. copy() - Returns a shallow copy of the list
# l1_copy = l1.copy()
# 12. min() - Returns the smallest element in the list
# min_value = min(l1)
# 13. max() - Returns the largest element in the list
# max_value = max(l1)
# 14. sum() - Returns the sum of all elements in the list (if they are numeric)
# sum_value = sum(l1)
# 15. len() - Returns the number of elements in the list
# length_of_l1 = len(l1)
# 16. slice - Returns a portion of the list
# sliced_l1 = l1[2:5]  # Returns elements from index 2 to 4
print(l1)